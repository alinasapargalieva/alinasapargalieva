#include <stdio.h>
#include <locale.h>
int main() {
	setlocale(LC_ALL, "Russian");
	float a, b, c;
	scanf_s("%f%f", &a, &b);
	c = (a + b) / 2;
	printf("Среднее арифметическое равно %f", c);
	return 0;
}
