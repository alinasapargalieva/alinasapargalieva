#include <stdio.h>
#include <locale.h>
int main() {
    setlocale(LC_ALL, "Russian");
    float i, f = 254.5;
     printf("Стоимость одного килограмма конфет %.3f\n", f);
     for (float i=0.1; i<1.0; i+=0.1 )
    printf("Стоимость %.1f килограмма конфет %.3f\n", i, f*i);
    return 0;
}
