#include <stdio.h>
#include <locale.h>
int main() {
    setlocale(LC_ALL, "Russian");
    int i = 1, N = 5, A = 0;
    for (i = 1; i <= N; i++) {
        A = A + (2 * i - 1);
        printf("Результат равен  %d\n", A);
    }
    return 0;
}
