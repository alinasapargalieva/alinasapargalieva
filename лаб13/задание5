#include <stdio.h>
#include <locale.h>
int main() {
    setlocale(LC_ALL, "Russian");
    int i, N = 5;
    float A = 5.5, x = 0, a = 1;
    for (i = 1; i <= N; i++) {
        a = a * A*(-1);
        x += a;
    }
    printf("Результат равен  %f\n", x);
    return 0;
}
